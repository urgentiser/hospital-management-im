import { createModuleService } from "@/services/modules/base-module.service";

export const multitouchCensusService = createModuleService({ moduleKey: "multitouch-census", basePath: "multitouch/census" });
